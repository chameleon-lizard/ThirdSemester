#include <stdio.h>

int
main(void)
{
    int one = 0;
    int two = 0;
    int three = 0;
    int four = 0;

    scanf("%d/%d %d:%d", &one, &two, &three, &four);

    printf("%d %d %d %d\n", one, two, three, four);
}
